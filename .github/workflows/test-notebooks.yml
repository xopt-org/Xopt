name: Test Notebooks

on:
  pull_request:
  push:
    branches-ignore:
      - main

jobs:
  test-notebooks:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        group: [1, 2, 3, 4]

    steps:
      - uses: actions/checkout@v4

      - name: Set up uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: "**/pyproject.toml"

      - name: Set up Python
        run: uv python install 3.12

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libopenmpi-dev ffmpeg

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Execute notebooks in parallel
        id: execute
        shell: bash
        env:
          GROUP: ${{ matrix.group }}
          TOTAL_GROUPS: 4
          SMOKE_TEST: "True"
        run: |
          set +e  # Don't exit on first error
          NOTEBOOKS=$(find . -type f -name "*.ipynb" -not -path '*/.*' -not -path '*Xparallel*')
          NOTEBOOK_ARRAY=($NOTEBOOKS)
          # Create temp directory for tracking
          TMPDIR=$(mktemp -d)
          trap "rm -rf $TMPDIR" EXIT
          SCRIPT_START=$(date +%s)
          # Function to execute a notebook and track results, with jitter + retries
          execute_notebook() {
              local file=$1
              local tmpdir=$2

              # Add small random jitter (0–2s) to desynchronize parallel starts
              sleep "$(awk -v seed="$RANDOM" 'BEGIN{srand(seed); printf "%.2f", rand() * 2}')"

              local START_TIME END_TIME DURATION
              local MAX_RETRIES=3
              local RETRY_COUNT=0
              local SUCCESS=false

              START_TIME=$(date +%s)

              while [ "$RETRY_COUNT" -lt "$MAX_RETRIES" ]; do
                  local LOG_FILE="/tmp/nbconvert_output_$$.$RETRY_COUNT.log"

                  if uv run jupyter nbconvert --to notebook --execute "$file" --inplace \
                        --ExecutePreprocessor.timeout=600 \
                        >"$LOG_FILE" 2>&1; then
                      SUCCESS=true
                      # On success, stream log to stdout for context if it exists
                      if [ -f "$LOG_FILE" ]; then
                          cat "$LOG_FILE"
                          rm -f "$LOG_FILE"
                      fi
                      break
                  fi

                  # If this looks like a transient kernel/port issue, retry with backoff
                  if [ -f "$LOG_FILE" ] && \
                     grep -qE "Address already in use|ZMQError|Kernel died before replying" "$LOG_FILE"; then
                      RETRY_COUNT=$((RETRY_COUNT + 1))
                      if [ "$RETRY_COUNT" -lt "$MAX_RETRIES" ]; then
                          local WAIT_TIME=$((2 ** RETRY_COUNT))  # 2,4,8s
                          echo "Port/kernel issue for $file, retrying in ${WAIT_TIME}s (attempt $((RETRY_COUNT + 1))/$MAX_RETRIES)..." >&2
                          sleep "$WAIT_TIME"
                          continue
                      else
                          echo "Failed to execute $file after $MAX_RETRIES attempts (kernel/port-related failure)" >&2
                          echo "--- Begin nbconvert log for $file ---" >&2
                          cat "$LOG_FILE" >&2
                          echo "--- End nbconvert log for $file ---" >&2
                      fi
                  else
                      # Non-transient error: show log and do not retry
                      if [ -f "$LOG_FILE" ]; then
                          echo "--- Begin nbconvert log for $file ---" >&2
                          cat "$LOG_FILE" >&2
                          echo "--- End nbconvert log for $file ---" >&2
                      fi
                  fi

                  break
              done

              END_TIME=$(date +%s)
              DURATION=$((END_TIME - START_TIME))

              if [ "$SUCCESS" = true ]; then
                  echo "$DURATION" > "$tmpdir/$(basename $file).status"
                  echo "✓ Successfully executed $file (${DURATION}s)"
              else
                  echo "failed:$DURATION" > "$tmpdir/$(basename $file).status"
                  echo "✗ Failed to execute $file (${DURATION}s)" >&2
              fi
          }
          export -f execute_notebook
          # Execute notebooks in this group in parallel (all notebooks in this group)
          for ((i=GROUP-1; i<${#NOTEBOOK_ARRAY[@]}; i+=TOTAL_GROUPS)); do
              execute_notebook "${NOTEBOOK_ARRAY[$i]}" "$TMPDIR" &
          done
          wait
          # Collect results
          TOTAL=0
          SUCCESS=0
          FAILED=0
          TOTAL_TIME=0
          echo "## Group $GROUP Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          for ((i=GROUP-1; i<${#NOTEBOOK_ARRAY[@]}; i+=TOTAL_GROUPS)); do
              file="${NOTEBOOK_ARRAY[$i]}"
              TOTAL=$((TOTAL + 1))
              status_file="$TMPDIR/$(basename $file).status"
              if [ -f "$status_file" ]; then
                  status=$(cat "$status_file")
                  if [[ "$status" == failed:* ]]; then
                      FAILED=$((FAILED + 1))
                      duration=${status#failed:}
                      TOTAL_TIME=$((TOTAL_TIME + duration))
                      echo "- ❌ \`$file\` (${duration}s)" >> $GITHUB_STEP_SUMMARY
                  else
                      SUCCESS=$((SUCCESS + 1))
                      TOTAL_TIME=$((TOTAL_TIME + status))
                      echo "- ✅ \`$file\` (${status}s)" >> $GITHUB_STEP_SUMMARY
                  fi
              fi
          done
          SCRIPT_END=$(date +%s)
          SCRIPT_DURATION=$((SCRIPT_END - SCRIPT_START))
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Summary for Group $GROUP:**" >> $GITHUB_STEP_SUMMARY
          echo "- Total: $TOTAL" >> $GITHUB_STEP_SUMMARY
          echo "- Success: $SUCCESS" >> $GITHUB_STEP_SUMMARY
          echo "- Failed: $FAILED" >> $GITHUB_STEP_SUMMARY
          echo "- Total execution time: ${TOTAL_TIME}s" >> $GITHUB_STEP_SUMMARY
          echo "- Wall clock time: ${SCRIPT_DURATION}s" >> $GITHUB_STEP_SUMMARY
          # Export for next step
          echo "failed=$FAILED" >> $GITHUB_OUTPUT
          echo "success=$SUCCESS" >> $GITHUB_OUTPUT
          echo "total=$TOTAL" >> $GITHUB_OUTPUT
          [ $FAILED -gt 0 ] && exit 1 || exit 0

      - name: Save results for summary
        if: always()
        run: |
          mkdir -p results
          echo "${{ steps.execute.outputs.success }},${{ steps.execute.outputs.failed }},${{ steps.execute.outputs.total }}" > results/group-${{ matrix.group }}-results.txt

      - name: Upload results artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: results-group-${{ matrix.group }}
          path: results/group-${{ matrix.group }}-results.txt

      - name: Minimize uv cache
        run: uv cache prune --ci

  summary:
    name: "Notebook Execution Summary"
    needs: test-notebooks
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Download all results
        uses: actions/download-artifact@v4
        with:
          pattern: results-group-*
          merge-multiple: true

      - name: Generate overall summary
        run: |
          echo "# 📊 Notebook Execution Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          TOTAL_SUCCESS=0
          TOTAL_FAILED=0
          TOTAL_NOTEBOOKS=0

          echo "## Results by Group" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Group | Total | Success ✅ | Failed ❌ |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|-------|-----------|----------|" >> $GITHUB_STEP_SUMMARY

          for i in 1 2 3 4; do
            if [ -f "group-$i-results.txt" ]; then
              IFS=',' read -r success failed total < "group-$i-results.txt"
              echo "| $i | $total | $success | $failed |" >> $GITHUB_STEP_SUMMARY
              TOTAL_SUCCESS=$((TOTAL_SUCCESS + success))
              TOTAL_FAILED=$((TOTAL_FAILED + failed))
              TOTAL_NOTEBOOKS=$((TOTAL_NOTEBOOKS + total))
            else
              echo "| $i | - | - | ⚠️ No results |" >> $GITHUB_STEP_SUMMARY
            fi
          done

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📈 Overall Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Total Notebooks**: $TOTAL_NOTEBOOKS" >> $GITHUB_STEP_SUMMARY
          echo "- **✅ Successful**: $TOTAL_SUCCESS" >> $GITHUB_STEP_SUMMARY
          echo "- **❌ Failed**: $TOTAL_FAILED" >> $GITHUB_STEP_SUMMARY

          if [ $TOTAL_FAILED -gt 0 ]; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "⚠️ **Some notebooks failed.** Check individual group summaries above for details." >> $GITHUB_STEP_SUMMARY
          else
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "🎉 **All notebooks executed successfully!**" >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "*Check individual group job summaries for detailed notebook-by-notebook results.*" >> $GITHUB_STEP_SUMMARY
